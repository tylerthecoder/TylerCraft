import { h } from "jsx-dom";

export const renderWorldPicker = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("clicked");
        }}
      />
    </div>
  );
};
